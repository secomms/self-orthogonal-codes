Scripts revolving self-orthogonal codes.

Files:

- `rank_distribution.sage`: compute sub-matrices rank distribution of non-systematic part;
- `sig-sizes.sage`: compute pk sizes for LESS, SPECK and ABL after compression;
- `test_LUP.ipynb`: jupyter notebook showing compression using LUP decomposition. 
